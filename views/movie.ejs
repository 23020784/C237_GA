<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  <title>TicketEase</title>
</head>
<body>
  <nav class="navbar navbar-expand-sm bg-dark navbar-dark">
    <div class="container-fluid">
      <a class="navbar-brand" href="/">Movie Catalogue</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#collapsibleNavbar">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="collapsibleNavbar">
        <ul class="navbar-nav">
          <li class="nav-item">
            <a class="nav-link" href="/addMovie">Add new movie</a>
          </li> 
          <li class="nav-item">
            <a class="nav-link" href="/">Category</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/about">About Us</a>
          </li>
        </ul>
      </div>
    </div>
  </nav>

  <div class="container">
    <br>
    <div class="text-center"><h2>Movie Details</h2></div>
    <br>
    <% if (movie) { %> 
        <div>  
            <p><b>Movie Name:</b><br> <%= movie.title %></p>
            <p><b>Movie Image:</b><br> <img src="/images/<%= movie.image %>" width="20%"></p>
            <p><b>Genre:</b><br> <%= movie.genre %></p>
            <p><b>Release Date:</b><br> <%= new Date(movie.release_date).toLocaleDateString('en-US', { year: 'numeric', month: 'short', day: 'numeric' }) %></p> 
            <p><b>Description:</b><br> <%= movie.description %></p>
            <p><b>Directors:</b><br> <%= movie.director %></p>
            <p><b>Cast:</b><br> <%= movie.cast %></p>
        </div>
    <% } else { %>
        <p>No movie found.</p>
    <% } %>
    <a href="/" class="btn btn-primary">Back</a>
  </div>

</body>
</html>
